Olá meus caros!

Seguindo o que foi pedido, esta é uma agenda de tarefas bem simples composta por um Crud utilizando o Mongo db e uma API para transacionar os dados. 

O ambiente contém:

webpack
eslint
prettier
Otimização (vendor)(optimizechunk)
Backend
nodejs
express
mongodb

* Para rodar as ferramentas basta buildar o container. Ferramentas executáveis no front temos o lint e o prettier e no back o jest (além das outras ferramentas obrigatórias). *
* O teste do lint é apenas para ver se o servidor está ouvindo na porta correta e retornando 200 *
Sugestão de ordem para correção:

cd DESAFIO_JS/ 
docker compose build 
docker compose up -d 
cd frontend
npm install
npm run lint
npm run format:check

cd ..

cd backend
npm install
npm run test

http://localhost/index.html -> Página inicial, aí basta testar o app.
